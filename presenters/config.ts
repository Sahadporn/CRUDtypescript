export const config = {
    port: 3000,
    mongoConfi: {
        user: "root",
        password: "root",
        host: "localhost",
        port: 27017,
        db: "student",
        collection: "profile"
    }
  }