export const config = {
    port: 3000,
    mongoConfig: {
        user: "root",
        password: "root",
        host: "localhost",
        port: 27017,
        db: "student",
        collection: "profile"
    }
  }